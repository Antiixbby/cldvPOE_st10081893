@model IEnumerable<cldvPOE_st10081893.Models.tbl_driver>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.driverAddress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tbl_person.personName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.driverAddress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tbl_person.personName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.driverID }) |
            @Html.ActionLink("Details", "Details", new { id=item.driverID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.driverID })
        </td>
    </tr>
}

</table>
